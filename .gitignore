venv/
__pycache__/
*.egg-info/
*.pyc
.laintas/
.parent_cmd
*.bak
build/**/*.deb
build/**/tmp/
build/linux/build/
build/linux/dist/
build/linux/dist-compat/
build/linux/dist-manylinux/
dist/
dist/
dist/

# laintas_cli_download (React/Vite)
laintas_cli_download/node_modules/
laintas_cli_download/dist/
laintas_cli_download/**/*_amd64.deb

# Generated architecture visualization (optional to commit)
architecture-zh.html
concepts-zh.html

# Release binaries are published to GitHub Releases, never committed. Two
# tarballs of one version reached 107 MB in the tree, and every sub-agent
# worktree is a full checkout — 31 of them multiplied that into 3.3 GB.
laintas_cli_download/public/releases/

# The pinned Windows Terminal download, cached between builds. Fetched by
# build_windows_package.sh and verified against a hash; never committed.
build/windows/.cache/
