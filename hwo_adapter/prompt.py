# AUTO-GENERATED by hwo/gen_prompts.py from hwo/prompt/*.txt — do not edit.

HWO_TOOL_DESCRIPTION = "Compile or run a .hwo multi-agent workflow file. Syntax: (prompt.md)#agent_name# { steps } for a named sub-agent with an optional custom system prompt (the prefix may also follow the name: #agent_name#(prompt.md) { steps }); append @model to the name to pin that agent to a specific backend model, e.g. #agent_name@glm-5.2# { steps } or (prompt.md)#agent_name@deepseek-v4-flash# { steps } — omit @model to use the session's selected model, and different agents in one workflow may run on different models; // ... // for a parallel block — '//' opens AND closes it, it is NOT a comment (comments are ``` ... ```); a parallel block may only contain complete agent definitions, max 6, and agent names must be unique among siblings (parallel members included). '->' between plain-text steps is a serial no-op separator — top-level steps run in order regardless; concurrency exists only inside // ... //. Nesting is supported: an agent's body may contain tasks, nested agents, and parallel blocks. Without a (prompt.md) prefix, an agent inherits the prompt override (if any) of whichever agent spawned it, else the normal agent prompt. Agents can message siblings/parent with agent_send/agent_receive and short-circuit their own result with agent_return. action=compile parses and validates without executing."
HWO_COMM_TOOLS = "Communication tools:\n  - agent_send(to, message)        -> send a message to a sibling or parent\n  - agent_receive(from, timeout?)  -> wait for a message from a specific member (default timeout 60s)\n  - agent_return(value)            -> report the final result to the parent and end this task"
HWO_SYNTAX = "HWO workflow syntax:\n  #name# { ... }              a named agent; its body runs as an ordered sequence\n  #name@model# { ... }        pin the agent to a backend model (e.g. #fe@glm-5.2#);\n                              omit @model to use the session's selected model\n  (prompt.md)#name# { ... }   bind a custom system prompt to the agent\n  #name#(prompt.md) { ... }   same, with the prefix after the name (alias)\n  // ... //                   PARALLEL block — '//' both opens and closes it\n  ->                          serial separator between plain-text steps (no-op)\n  ``` ... ```                 comment block\n  <plain text>                a task, executed by one agent\n\nPITFALLS (the mistakes every first-time author makes — read before writing):\n  1. '//' is NOT a comment. It is the parallel-block delimiter. Comments use ``` ... ```.\n  2. Top-level steps ALWAYS run serially, with or without '->'. Concurrency exists\n     ONLY inside a // ... // block.\n  3. A parallel block may only contain complete agent definitions\n     (#name# { ... }, prompt prefix allowed), max 6 — no bare tasks.\n  4. Agent names must be unique among siblings in the same scope, parallel members\n     included. Define each agent once, at the place where it runs — do not declare\n     it outside a parallel block and then reference the name inside.\n\nExample — research and build run in parallel, then one integrator merges:\n  ``` two workers concurrently, then merge ```\n  //\n    (researcher.md)#research# { collect the comparison data into data.json }\n    #builder# { write page.html against the agreed schema with placeholder data }\n  //\n  #integrator# { inject data.json into page.html and verify it renders }\n\nAgents coordinate with agent_send / agent_receive / agent_return."
