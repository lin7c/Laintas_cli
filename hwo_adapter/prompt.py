# AUTO-GENERATED by hwo/gen_prompts.py from hwo/prompt/*.txt — do not edit.

HWO_TOOL_DESCRIPTION = "Compile or run a .hwo multi-agent workflow file. Syntax: (prompt.md)#agent_name# { steps } for a named sub-agent with an optional custom system prompt (the prefix may also follow the name: #agent_name#(prompt.md) { steps }); // #a# {...} #b# {...} // for a parallel block (agent entries only, max 6); '->' between plain-text steps for serial order (a no-op separator — steps run in order either way); ``` ... ``` for comments. Nesting is supported: an agent's body may contain tasks, nested agents, and parallel blocks. Without a (prompt.md) prefix, an agent inherits the prompt override (if any) of whichever agent spawned it, else the normal agent prompt. Agents can message siblings/parent with agent_send/agent_receive and short-circuit their own result with agent_return. action=compile parses and validates without executing."
HWO_COMM_TOOLS = "Communication tools:\n  - agent_send(to, message)        -> send a message to a sibling or parent\n  - agent_receive(from, timeout?)  -> wait for a message from a specific member (default timeout 60s)\n  - agent_return(value)            -> report the final result to the parent and end this task"
HWO_SYNTAX = "HWO workflow syntax:\n  #name# { ... }              a named agent; its body runs as an ordered sequence\n  (prompt.md)#name# { ... }   bind a custom system prompt to the agent\n  #name#(prompt.md) { ... }   same, with the prefix after the name (alias)\n  // #a# {...} #b# {...} //    parallel block — agent entries only, max 6\n  ->                          serial separator between plain-text steps (no-op)\n  ``` ... ```                 comment block\n  <plain text>                a task, executed by one agent\nAgents coordinate with agent_send / agent_receive / agent_return."
